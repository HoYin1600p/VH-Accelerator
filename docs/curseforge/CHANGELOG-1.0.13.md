## 1.0.13

**Changed**

- Update notices now default to showing critical releases only.
- Added the `updates.updateTypes` config option with `CRITICAL` and `ALL`
  choices.
- Added `/vha updates critical` and `/vha updates all`; changes apply
  immediately to both menu and chat notices.
